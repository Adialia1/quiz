import { Progress as GluestackProgress, ProgressFilledTrack as GluestackProgressFilledTrack } from '@gluestack-ui/themed';

export const Progress = GluestackProgress;
export const ProgressFilledTrack = GluestackProgressFilledTrack;
