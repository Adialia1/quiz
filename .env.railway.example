# Railway Environment Variables Template
# Copy these to Railway Dashboard > Your Service > Variables tab

# ============================================================================
# AI & LLM Configuration
# ============================================================================
OPENROUTER_API_KEY=your_openrouter_api_key_here
EMBEDDING_MODEL=intfloat/multilingual-e5-large

# ============================================================================
# Supabase Vector Database Configuration
# ============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_KEY=your_service_role_key_here

# ============================================================================
# Clerk Authentication (Backend)
# ============================================================================
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret

# ============================================================================
# Notifications
# ============================================================================
NOTIFICATION_API_KEY=your_notification_api_key_here

# ============================================================================
# RevenueCat Monetization (Optional)
# ============================================================================
REVENUE_CAT_API_KEY=your_revenuecat_api_key

# ============================================================================
# Redis Cache Configuration
# ============================================================================
REDIS_ENABLED=true

# Option 1: Use Railway's Redis service (if using Railway Redis)
# Railway auto-provides REDIS_URL when you add Redis service
# REDIS_URL=${{Redis.REDIS_URL}}

# Option 2: Use Upstash Redis (external managed Redis)
# Upstash Redis URL format: rediss://default:PASSWORD@HOST:PORT (use rediss:// for TLS)
# For your Upstash Redis instance:
REDIS_URL=rediss://default:AWtmAAIncDIwOGJiZWU5NzlmODY0ZjI1OTI0OWRhYjViZDAwNTc4OHAyMjc0OTQ@possible-worm-27494.upstash.io:6379

# Optional: Manual configuration (if not using REDIS_URL)
# REDIS_HOST=possible-worm-27494.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=AWtmAAIncDIwOGJiZWU5NzlmODY0ZjI1OTI0OWRhYjViZDAwNTc4OHAyMjc0OTQ
# REDIS_DB=0
# REDIS_TLS=true  # Upstash requires TLS

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
# Railway auto-provides DATABASE_URL when you add PostgreSQL service
# Use Railway's service reference (recommended):
POSTGRES_URL=${{Postgres.DATABASE_URL}}

# Or use external PostgreSQL:
# POSTGRES_URL=postgresql://user:password@host:5432/database

# Database Connection Pool Settings (Optional - uses defaults if not set)
DB_MIN_POOL_SIZE=5
DB_MAX_POOL_SIZE=20
DB_COMMAND_TIMEOUT=60

# ============================================================================
# Railway Auto-Generated Variables (DO NOT SET MANUALLY)
# ============================================================================
# These are automatically provided by Railway:
# - PORT (Railway sets this, your app listens on it)
# - RAILWAY_ENVIRONMENT (production/staging)
# - RAILWAY_PROJECT_ID
# - RAILWAY_SERVICE_ID
# - DATABASE_URL (from PostgreSQL service)
# - REDIS_URL (from Redis service)

# ============================================================================
# NOTES
# ============================================================================
# 1. Set these in Railway Dashboard > Variables tab, NOT in code
# 2. Use ${{ServiceName.VARIABLE}} to reference other Railway services
# 3. Railway encrypts all environment variables
# 4. Changes to variables trigger a new deployment
# 5. Never commit actual secrets to Git
